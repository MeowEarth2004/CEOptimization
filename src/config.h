#pragma once
#include <WiFi.h>

const char* ssid = "YOUR_WIFI";        // ใส่ชื่อ WiFi
const char* password = "YOUR_PASS";    // ใส่รหัส WiFi
const char* mqtt_server = "192.168.1.100";  // IP ของคอมที่รัน MQTT Broker
const int mqtt_port = 1883;
